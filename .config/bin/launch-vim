#!/bin/sh

#
# Could potentially do this with autocmd
# :autocmd BufWinEnter * kitty @ set-colors background=$LIGHT_BORDER
# :autocmd BufWinLeave * kitty @ set-colors background=$DARK_BORDER
# 

# source "$COLORSCHEMES/current/colors-nvim-borders"

# pid=$(xprop -id $(xprop -root | awk '/_NET_ACTIVE_WINDOW\(WINDOW\)/{print $NF}') | awk '/_NET_WM_PID\(CARDINAL\)/{print $NF}')

# kitty @ --to unix:@kitty.sock-$pid set-colors background=$LIGHT_BORDER

nvim $@

# kitty @ --to unix:@kitty.sock-$pid set-colors background=$DARK_BORDER
